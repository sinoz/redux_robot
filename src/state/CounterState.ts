/**
 * The counter that holds a numeric counting value.
 */
export type CounterState = {
    value: number
}

/**
 * The initial state of the `CounterState`.
 */
export let initialCounterState = { value: 0 }